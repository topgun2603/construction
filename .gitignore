node_modules/
dist/
dist-worker/
build/
.next/
out/
coverage/
.turbo/
*.tsbuildinfo

.env
.env.*
# Templates only. `!.env.example` un-ignores that exact name and nothing else, so
# `.env.prod.example` stayed invisible to git — it had to be named here too.
!.env.example
!.env.prod.example
!*.env.example

# Secrets. Firebase Admin service account keys are private keys â€” never commit one.
*service-account*.json
*serviceaccount*.json
*-firebase-adminsdk-*.json
secrets/
google-credentials.json

# Flutter
apps/mobile/.dart_tool/
apps/mobile/build/
apps/mobile/.flutter-plugins
apps/mobile/.flutter-plugins-dependencies
apps/mobile/ios/Pods/

# local infra volumes
infra/.data/

.DS_Store
Thumbs.db
*.log
.claude/settings.json
