<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        The splash icon is the PNG that flutter_native_splash generates from `assets/launcher_adaptive.png`,
        which is what `dart run flutter_native_splash:create` writes here by default — so this file
        survives a regeneration untouched.

        It does not animate. Android 12 and later will only animate a splash icon that is a
        drawable it can transform, which means a vector; a bitmap is shown still. `splash_logo.xml`
        and its animators are the traced vector that did animate, kept in case that is wanted back.
    -->
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowSplashScreenBackground">#FFFFFF</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/android12splash</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.
         
         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
